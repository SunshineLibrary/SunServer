<%= form_for(@provider) do |f| %>
  <% if @provider.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@provider.errors.count, "error") %> prohibited
        this post from being saved:</h2>
    <ul>
    <% @provider.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :intro %><br />
    <%= f.text_area :intro%>
  </div>
  <div class="field">
    <%= f.label :logo_path %><br />
    <%= f.text_field :logo_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>