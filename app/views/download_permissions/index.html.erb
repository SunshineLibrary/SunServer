<h1>Listing download_permissions</h1>

<table>
  <tr>
    <th>Owner</th>
    <th>Owner type</th>
    <th>Resource</th>
    <th>Resource type</th>
    <th>Permission boolean</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @download_permissions.each do |download_permission| %>
  <tr>
    <td><%= download_permission.owner_id %></td>
    <td><%= download_permission.owner_type %></td>
    <td><%= download_permission.resource_id %></td>
    <td><%= download_permission.resource_type %></td>
    <td><%= download_permission.permission_boolean %></td>
    <td><%= link_to 'Show', download_permission %></td>
    <td><%= link_to 'Edit', edit_download_permission_path(download_permission) %></td>
    <td><%= link_to 'Destroy', download_permission, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Download permission', new_download_permission_path %>
