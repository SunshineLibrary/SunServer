<h1>Listing pdf_activities</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pdf_activities.each do |pdf_activity| %>
  <tr>
    <td><%= link_to 'Show', pdf_activity %></td>
    <td><%= link_to 'Edit', edit_pdf_activity_path(pdf_activity) %></td>
    <td><%= link_to 'Destroy', pdf_activity, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pdf activity', new_pdf_activity_path %>
