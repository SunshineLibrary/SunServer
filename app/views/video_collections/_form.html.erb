<%= form_for(@video_collection) do |f| %>
  <% if @video_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_collection.errors.count, "error") %> prohibited this video_collection from being saved:</h2>

      <ul>
      <% @video_collection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :intro %><br />
    <%= f.text_field :intro %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
